import styled from "styled-components";

export const Section = styled.section`
    display: flex;
    position: relative;
    height: calc(100vh - 60px);
    width: 100%;
    max-width: 1900px;
    margin: auto;
`;
